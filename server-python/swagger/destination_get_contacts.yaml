parameters:
  - name: destination_id
    in: path
    description: "ID of the destination"
    schema:
      type: string
      example: "2"
responses:
  "200":
    description: "List of destinations"
  "404":
    description: "Not found"
definitions:
  DestinationGetContactsRequest:
    type: object
    properties:
      email:
        type: string
        example: ""