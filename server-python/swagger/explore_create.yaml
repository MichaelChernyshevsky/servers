parameters:
  - in: body
    name: sendResetLinkRequest
    type: Object
    schema:
      $ref: "#/definitions/SendResetLinkRequest"
responses:
  "200":
    description: "Send reset password link to user"
  "404":
    description: "Not found"
definitions:
  SendResetLinkRequest:
    type: object
    properties:
      email:
        type: string
        example: ""